#+TITLE: Unified Rust DataScript Core
#+AUTHOR: RJ Sheperd
#+DATE: 2026-03-12

* Context

The CLJS/Rust interop boundary is broken: =._storage= inaccessible on
WasmPSS, =equals= always false, JsStorage double-crossing the WASM
boundary. Move the datascript core into Rust to eliminate these problems.

* Phase 1: Native Datom Keys

** 1.1 Unify Key type

- *File:* =rust/persistent-sorted-set/src/key.rs=
- Change ~#[cfg(target_arch = "wasm32")] pub type Key = JsValue~ to ~pub type Key = Datom~ (for WASM)
- Keep native ~Key = i64~ for now (or also switch to Datom)
- *Test:* ~cargo test~ — all existing native tests still compile

** 1.2 Replace Comparator with IndexType

- *File:* =rust/persistent-sorted-set/src/node.rs=
- Change ~Comparator = dyn Fn(&Key, &Key) -> Ordering~ to store ~IndexType~ enum
- Call ~cmp_datoms(index_type, &d1, &d2)~ from =comparator.rs= directly
- Update ~PersistentSortedSet~ to store ~IndexType~ instead of ~Rc<Comparator>~
- *Files also affected:* =set.rs=, =branch.rs=, =leaf.rs=, =seq.rs=
- *Test:* ~cargo test~ — existing comparator tests pass; new test comparing datoms through PSS conj/disj

** 1.3 Datom ↔ JsValue conversion

- *File:* =rust/persistent-sorted-set/src/wasm.rs=
- Add ~datom_from_js(val: &JsValue) -> Datom~ — extracts =e=, =a=, =v=, =tx= fields from CLJS Datom deftype
  - =a= (keyword): parse ~"person/name"~ → ~Attr::Keyword { ns: Some("person"), name: "name" }~
  - =v=: detect JS type → ~Value::Long~, ~Value::Str~, ~Value::Bool~, etc.
- Add ~datom_to_js(d: &Datom) -> JsValue~ — creates JS object with =e=, =a=, =v=, =tx= properties
- *Test (Rust):* ~#[wasm_bindgen_test]~ round-trip: create JS datom → ~datom_from_js~ → ~datom_to_js~ → verify fields match
- *Test (Rust):* All value types: Long, Double, Str, Bool, Keyword, Ref, Nil

** 1.4 New WasmPSS constructors

- *File:* =rust/persistent-sorted-set/src/wasm.rs=
- ~WasmPSS::empty_with_index(index_type: String)~ — creates PSS with Rust comparator
- ~WasmPSS::conj_datom(e, a, v, tx)~ — converts args to Datom, conjs into tree
- ~WasmPSS::disj_datom(e, a, v, tx)~ — converts args to Datom, disjs from tree
- ~WasmPSS::datoms_slice(from_e, from_a, ..., to_e, to_a, ...) -> Array~ — slice returning JS datom array
- Keep old ~conj(key, cmp)~ temporarily for backward compat
- *Test (Rust):* ~#[wasm_bindgen_test]~ create EAVT set, conj 3 datoms, verify count and seq order

** 1.5 Update CLJS wrapper

- *File:* =src/absurder_sql/datascript/persistent_sorted_set.cljs=
- ~sorted-set*~ uses ~WasmPSS.empty_with_index~ when opts contains =:index-type=
- ~conj~ calls ~.conj_datom~ with extracted fields instead of ~.conj~ with JS comparator
- ~disj~ calls ~.disj_datom~ similarly
- Keep old path as fallback
- *Test (CLJS):* Existing kaocha storage tests pass with new constructors

** 1.6 Wire into db.cljc

- *File:* =src/absurder_sql/datascript/db.cljc=
- ~empty-db~ passes =:index-type "eavt"/"aevt"/"avet"= in opts to ~sorted-set*~
- *Test (CLJS):* ~(d/empty-db)~ creates DB with Rust-compared indexes; ~(d/db-with db [[:db/add 1 :name "Ivan"]])~ works

** Phase 1 integration test

- ~cargo test~ passes (all Rust tests)
- ~wasm-pack build~ succeeds
- ~bun shadow-cljs compile :test-kaocha && ./bin/kaocha~ — storage_test and interface_test pass

* Phase 2: WASM SQLiteStorage

** Context

Phase 1 established Rust-native datom keys and comparators, but storage
still crosses the WASM boundary twice per node (Rust → JS → CLJS → JS →
Rust) via JsStorage. Phase 2 gives Rust direct SQLite access on WASM so
that B+ tree node persistence happens entirely in Rust, eliminating the
JsStorage bridge.

*Key constraint:* =sqlite-wasm-rs= is a low-level C FFI binding
(synchronous, ~unsafe~), NOT API-compatible with =rusqlite=. The WASM
implementation must use raw ~sqlite3_*~ functions, not rusqlite's
~Connection~ / ~Statement~ types.

*Approach:* Transitional — Rust handles B+ tree node storage in SQLite
directly; CLJS metadata (schema, max-eid, max-tx, index root addresses,
tail) still stored via the existing CLJS StorageAdapter path. This keeps
the CLJS conn/transact flow unchanged while eliminating the hot-path JS
boundary crossings for node store/restore.

** 2.1 Datom binary serialization

- *New file:* =rust/persistent-sorted-set/src/datom_serde.rs=
- Shared by both native (rusqlite) and WASM (sqlite-wasm-rs) targets
- ~serialize_datom(d: &Datom) -> Vec<u8>~ and ~deserialize_datom(bytes: &[u8]) -> Datom~
- Format: ~[e:i64_le][a_type:u8][a_len:u32_le][a_bytes:utf8][v_type:u8][v_data:var][tx:i64_le]~
  - Attribute types: 0=None, 1=Keyword(no ns), 2=Keyword(with ns), 3=Str
  - Value types: 0=Nil, 1=Bool, 2=Long, 3=Double, 4=Str, 5=Keyword, 6=Ref, 7=Instant, 8=Uuid, 9=Bytes
- ~serialize_keys(keys: &[Datom]) -> Vec<u8>~ — length-prefixed concatenated datoms
- ~deserialize_keys(blob: &[u8]) -> Vec<Datom>~ — inverse
- ~serialize_addrs~ / ~deserialize_addrs~ — packed i64 LE (reuse existing pattern from =sqlite_storage.rs=)
- *Test:* Round-trip every Value variant; round-trip datoms with various attr types
- *Reuse:* Existing ~sqlite_storage.rs::{serialize_addrs, deserialize_addrs}~ pattern (promote to shared module)

** 2.2 Refactor native SQLiteStorage to use datom_serde

- *File:* =rust/persistent-sorted-set/src/sqlite_storage.rs=
- Replace inline ~serialize_keys~ / ~deserialize_keys~ (which currently pack i64s for native ~Key = i64~) with calls to ~datom_serde::serialize_keys~ / ~deserialize_keys~
- *Note:* On native, ~Key = i64~ — this step only applies if we unify Key to Datom on native too. If Key stays i64 on native, keep the existing i64 serialization but still extract ~serialize_addrs~ / ~deserialize_addrs~ to =datom_serde.rs=.
- *Test:* ~cargo test~ — existing native storage tests still pass

** 2.3 WASM SQLiteStorage

- *New file:* =rust/persistent-sorted-set/src/wasm_sqlite_storage.rs= (wasm32-only)
- Uses ~sqlite_wasm_rs::export::*~ C FFI functions (~sqlite3_open_v2~, ~sqlite3_prepare_v2~, ~sqlite3_step~, ~sqlite3_bind_*~, ~sqlite3_column_*~, ~sqlite3_finalize~, ~sqlite3_close_v2~)
- Same table schema: ~pss_nodes(addr INTEGER PRIMARY KEY, level INTEGER NOT NULL, keys BLOB NOT NULL, addrs BLOB)~
- Implements ~IStorage~ trait (same as native ~SQLiteStorage~)
- ~WasmSQLiteStorage::new(db_path: &str, settings: Settings) -> Self~
  - Opens with ~sqlite3_open_v2(path, SQLITE_OPEN_READWRITE | SQLITE_OPEN_CREATE, ...)~
  - Creates table if not exists
  - Queries ~MAX(addr)~ for next_addr
- ~store(&mut self, node: &Node) -> i64~ — INSERT using ~datom_serde::serialize_keys~, ~datom_serde::serialize_addrs~
- ~restore(&self, address: i64) -> Rc<Node>~ — SELECT + deserialize
- ~list_addresses~ / ~delete~ — standard SQL operations
- Includes LRU cache (reuse =lru= crate, same pattern as native)
- *Test:* ~#[wasm_bindgen_test]~ create storage, store nodes, restore, verify

** 2.4 Connection pool

- *File:* =rust/persistent-sorted-set/src/wasm_sqlite_storage.rs= (or new =connection.rs=)
- ~thread_local! { RefCell<HashMap<String, *mut sqlite3>> }~
- ~open_or_get(db_path: &str) -> *mut sqlite3~ — reuse open connections
- ~close_all()~ — cleanup for teardown
- *Safety:* Raw pointer stored because =sqlite-wasm-rs= returns ~*mut sqlite3~
- *Test:* Open same path twice → same pointer; different paths → different pointers

** 2.5 New WasmPSS constructors for SQLite

- *File:* =rust/persistent-sorted-set/src/wasm.rs=
- ~WasmPSS::withSqlite(index_type: String, db_path: String, settings: JsValue) -> WasmPSS~
  - Creates ~WasmSQLiteStorage~, wraps in ~PersistentSortedSet::with_storage~
- ~WasmPSS::restoreFromSqlite(index_type: String, address: f64, db_path: String, settings: JsValue) -> WasmPSS~
  - Creates ~WasmSQLiteStorage~, calls ~PersistentSortedSet::restore~
- Keep existing JsStorage-based constructors as fallback
- *Test (WASM):* Create set with SQLite, conj datoms, store, restore, verify

** 2.6 Update CLJS wrappers

- *File:* =src/absurder_sql/datascript/persistent_sorted_set.cljs=
- Add ~sorted-set-sqlite*~ that calls ~.withSqlite~ instead of ~.emptyWithIndexAndStorage~
- Add ~restore-by-sqlite~ that calls ~.restoreFromSqlite~
- *File:* =src/absurder_sql/datascript/storage.cljs=
- Update ~StorageAdapter~ to use new SQLite-backed PSS constructors when a SQLite db-path is available in opts
- Metadata (schema, max-eid, max-tx, root addrs, tail) still stored via CLJS IStorage path
- *File:* =src/absurder_sql/datascript/db.cljc=
- ~empty-db~ passes =:db-path= through opts when SQLite storage requested

** 2.7 Delete JsStorage (deferred)

- *DO NOT delete yet* — JsStorage is still needed as fallback for non-SQLite storage (e.g. memory-storage in tests, localStorage)
- Mark ~JsStorage~ methods as deprecated
- Delete in Phase 3 when all storage goes through Rust

** Phase 2 verification

1. ~cargo test~ — native tests pass (datom_serde round-trips)
2. ~wasm-pack build --target web --no-typescript~ — WASM builds
3. ~bun shadow-cljs compile :test-kaocha && ./bin/kaocha~ — storage_test passes (JsStorage fallback)
4. Manual test: datascript_client with SQLite storage → create db, transact, store, restore

* Phase 3: WasmDB

** 3.1 Schema types in Rust

- *New file:* =rust/persistent-sorted-set/src/schema.rs=
- ~Schema = HashMap<Attr, AttrSchema>~ where ~AttrSchema~ has fields for =:db/cardinality=, =:db/valueType=, =:db/unique=, =:db/index=, =:db/isComponent=
- ~ReverseSchema~ groups attrs by property (e.g., ~multival_attrs: HashSet<Attr>~, ~ref_attrs: HashSet<Attr>~, ~indexed_attrs: HashSet<Attr>~)
- ~schema_from_js(JsValue) -> Schema~ — parses CLJS schema map
- *Test:* Parse example schema with refs, many-cardinality, unique attrs; verify rschema groupings

** 3.2 Database struct

- *New file:* =rust/persistent-sorted-set/src/db.rs=
- ~Database { schema, eavt, aevt, avet, max_eid, max_tx, rschema }~
- ~Database::empty(schema) -> Database~ — creates 3 empty PSS indexes
- ~Database::with_datom(datom, added) -> Database~ — conj/disj into appropriate indexes based on schema (indexed attrs go into avet)
- ~Database::with_datoms(datoms: &[(Datom, bool)]) -> Database~ — batch version
- *Test:* Empty db + with_datom for non-indexed attr → present in eavt+aevt, absent from avet
- *Test:* Empty db + with_datom for indexed attr → present in all 3
- *Test:* Retraction removes from all relevant indexes

** 3.3 Database query methods

- ~Database::datoms(index_type, c0, c1, c2, c3) -> Vec<Datom>~ — slice index by components
- ~Database::search(e, a, v, tx) -> Vec<Datom>~ — search for matching datoms (used by entity API)
- ~Database::count() -> usize~
- *Test:* Add 10 datoms, query by entity, by attribute, by value; verify results

** 3.4 Database storage

- ~Database::store(db_name) -> Result<()>~ — stores all 3 indexes + metadata row (addr=0: schema, max_eid, max_tx, eavt_root, aevt_root, avet_root)
- ~Database::restore(db_name) -> Result<Database>~ — reads metadata, lazy-restores indexes
- ~Database::collect_garbage(db_name) -> Result<()>~ — walk all 3 indexes, delete orphan addresses
- *Test:* Store db with 100 datoms, restore, verify equality
- *Test:* Store, modify, store again, GC, verify orphans removed

** 3.5 WasmDB exports

- *File:* =rust/persistent-sorted-set/src/wasm.rs=
- ~WasmDB::empty(schema: JsValue) -> WasmDB~
- ~WasmDB::with_datoms(datoms_array: Array) -> WasmDB~
- ~WasmDB::datoms(index: String, c0, c1, c2, c3) -> Array~
- ~WasmDB::search(e, a, v, tx) -> Array~
- ~WasmDB::store(db_name: String) -> Promise~
- ~WasmDB::restore(db_name: String) -> Promise<WasmDB>~
- ~WasmDB::collect_garbage() -> Promise~
- Accessors: ~schema()~, ~max_eid()~, ~max_tx()~, ~count()~
- *Test (WASM):* Full lifecycle: empty → with_datoms → store → restore → datoms query → verify

** 3.6 CLJS DB wrapper

- *File:* =src/absurder_sql/datascript/db.cljc=
- Add ~_wasm_db~ field to DB record (or use metadata)
- ~ISearch~ delegates to ~(.search wasm-db e a v tx)~
- ~ICounted~ delegates to ~(.count wasm-db)~
- ~empty-db~ creates ~WasmDB.empty(schema)~ and wraps it
- *Test (CLJS):* ~(d/empty-db {:name {:db/index true}})~ creates DB backed by WasmDB

** 3.7 Simplify CLJS storage layer

- *File:* =src/absurder_sql/datascript/storage.cljs=
- ~store-impl!~ → ~(.store wasm-db db-name)~
- ~restore-impl~ → ~WasmDB.restore(db-name)~
- Remove: ~StorageAdapter~, ~*store-buffer*~, ~gen-addr~, ~remember-db~, ~stored-dbs~ WeakRef machinery
- *File:* =src/absurder_sql/datascript/storage_async.cljs=
- Remove: ~SyncStorageWrapper~, ~AsyncStorageAdapter~, ~prefetch-tree!~
- *File:* =src/absurder_sql/datascript/conn.cljc=
- ~storage-adapter~ reads from WasmDB instead of ~(.-_storage (:eavt db))~
- *Test (CLJS):* Full kaocha test suite passes

** Phase 3 integration test

- ~cargo test~ passes
- ~wasm-pack build~ succeeds
- All kaocha tests pass: storage_test, async_storage_test, interface_test
- Test: transact 1000 datoms, store to SQLite, restore, query, verify results match

* Phase 4: Feature Parity

Phase 4 closes the gap between the Rust engine and the CLJS DataScript engine so
that Rust can serve as a complete drop-in replacement. Tasks are ordered by
production impact.

** DONE 4.1 Aggregates in Rust [HIGH]

- *New file:* =rust/persistent-sorted-set/src/aggregates.rs=
- All 12 built-in aggregates implemented: =sum=, =avg=, =median=, =variance=,
  =stddev=, =min=, =max=, =rand=, =sample=, =count=, =count-distinct=, =distinct=
- =FindElement::Aggregate= variant added to =query_parser.rs= with =(name ?var)=
  and =(name n ?var)= forms
- Aggregation pipeline in =aggregates::aggregate()=: group by non-aggregate columns →
  apply aggregate functions per group → return one row per group
- Wired into =query_edn= in =wasm_datascript.rs= between query resolution and output
- CLJS fallback removed: =unsupported-find?= in =impl_rust.cljs= now returns false
- *Tests:* 13 unit tests in =aggregates.rs= + 16 integration tests in =parser_tests.rs=
  (parse, grouped count/sum, avg, median, min/max, count-distinct, all-12-parse)

** DONE 4.2 fn-expr support [HIGH]

- *New file:* =rust/persistent-sorted-set/src/fn_expr.rs=
- =Clause::FnExpr { name, args, binding }= variant added to =relation.rs=
- Parser now produces =FnExpr= instead of silently skipping fn-expr clauses
- =apply_fn_expr= evaluates function per-tuple and binds result to output variable
- ~50 built-in functions implemented in =eval_fn=:
  - Arithmetic: =+=, =-=, =*=, =/=, =quot=, =rem=, =mod=, =inc=, =dec=, =abs=, =max=, =min=
  - String: =str=, =subs=, =count=
  - Regex: =re-find=, =re-matches=
  - Type/keyword: =name=, =namespace=, =keyword=, =long=, =double=
  - Predicates (as fns): =zero?=, =pos?=, =neg?=, =even?=, =odd?=, =true?=, =false?=,
    =nil?=, =some?=, =number?=, =string?=, =keyword?=, =empty?=
  - Logic: =not=, =identity=, =ground=
  - Comparison: ===, =!==, =>=, =<=, =>==, =<==, =compare=
  - String predicates: =clojure.string/blank?=, =clojure.string/includes?=,
    =clojure.string/starts-with?=, =clojure.string/ends-with?=
- *Tests:* 20 unit tests in =fn_expr.rs= + 8 integration tests in =parser_tests.rs=
  (parse, str concat, arithmetic, name/keyword, chained, fn-expr+predicate, ground)

** DONE 4.3 Extended predicates [MEDIUM]

- *File:* =rust/persistent-sorted-set/src/relation.rs= (=apply_predicate_filter=)
- Fast path retained for the original 8 predicates (=>= =<= =>== =<== === ==== =!==
  =not== =-differ?=)
- Extended predicates now delegate to =fn_expr::eval_fn= for ~40+ predicates:
  - Numeric: =zero?=, =pos?=, =neg?=, =even?=, =odd?=
  - Type checks: =true?=, =false?=, =nil?=, =some?=, =number?=, =string?=, =keyword?=
  - Logic: =not=
  - Collection: =empty?=
  - Regex: =re-find=, =re-matches=
  - String: =clojure.string/starts-with?=, =clojure.string/ends-with?=,
    =clojure.string/includes?=, =clojure.string/blank?=
- Known functions that return =None= (e.g. =re-find= no match) are treated as
  falsy. Unknown predicates still pass through (unchanged behavior for custom fns).
- *Tests:* 13 integration tests in =parser_tests.rs= (zero, pos/neg, even/odd,
  nil/some, string/keyword, not, re-find, re-matches, starts-with, includes,
  ends-with, number, equality aliases)

** 4.4 Query features: =:with=, return maps, binding forms [MEDIUM — partially superseded]

/Note:/ Behave audit (Phase 5) found =:with= and return maps are *unused*.
Input binding forms are needed and tracked as Phase 5.2.

*** =:with= clause — NOT NEEDED (zero behave usage)

- =:with [?vars]= includes variables in joins but excludes from final results
- Required for correct aggregate grouping (4.1)
- Parse =:with= in =parse_query=; pass through to =resolve_query=; strip after aggregation

*** Return maps — NOT NEEDED (zero behave usage)

- CLJS supports =:keys=, =:syms=, =:strs= in queries to return maps instead of tuples
- Parse in =parse_query=; apply in =tuples_to_js= (wrap tuples as JS objects)

*** Input binding forms → moved to Phase 5.2

- Current =bind_inputs= only handles scalar bindings
- Collection binding =[?a ...]= is heavily used in behave (~15 queries)
- See Phase 5.2 for full implementation plan

** 4.5 Pull attribute options [MEDIUM — NOT NEEDED for behave]

/Note:/ Behave audit found zero usage of =:as=, =:default=, =:limit=, =:xform=
in pull patterns. Deprioritized.

- *Files:* =rust/persistent-sorted-set/src/pull_parser.rs=, =pull.rs=

** 4.6 Pull recursion depth control [LOW — NOT NEEDED for behave]

/Note:/ Behave audit found zero usage of recursive pull patterns. Deprioritized.

- *Files:* =rust/persistent-sorted-set/src/pull_parser.rs=, =pull.rs=

** DONE 4.7 Entity API [LOW]

- *File:* =src/absurder_sql/datascript/impl_rust.cljs=
- *Approach:* CLJS-side =RustDBProxy= deftype that implements =ISearch=,
  =IIndexAccess=, and =IDB= — delegating to the Rust =WasmDataScript= instance.
  The existing CLJS =Entity= deftype works unchanged on top of this proxy.
- =RustDBProxy= fields: =rdb= (WasmDataScript), =schema=, =rschema=
  - =-search= → =.search()= on Rust DB, converts JS datoms to CLJS Datom records
  - =-datoms= / =-seek-datoms= → =.datomsIndex()= on Rust DB
  - =-schema= → returns CLJS schema map
  - =-attrs-by= → returns attr sets from rschema (for =multival?=, =ref?=, =component?=)
- =entity= function: wraps Rust DB in =RustDBProxy=, calls =de/entity=
- =touch= function: delegates to =de/touch= (recursively loads + touches components)
- Supports: lazy attribute access, reverse refs (=:_parent=), component entities,
  lookup refs, =:db/id= access, ILookup/IFn/ISeqable/ICounted protocols

** DONE 4.8 Transact in Rust [HIGH]

*** Architecture

=TransactableDB= trait abstracts over =DataScriptDB= (native test) and
=WasmDataScript= (WASM production). Core transact logic lives in
=persistent-sorted-set/src/transact.rs= and is testable via =cargo test=
without WASM.

*** Transaction data types

- =TxEntity= — =Add=, =Retract=, =RetractAttribute=, =RetractEntity=, =MapEntity=
- =EntityRef= — =Eid(i64)=, =TempId(Neg|Str)=, =LookupRef(attr, val)=, =CurrentTx=
- =TxValue= — =Val(Value)=, =TempId=, =LookupRef=, =CurrentTx=, =Nested(Box<TxEntity>)=
- =TxReport= — =tx_data: Vec<Datom>=, =tempids: HashMap<TempId, i64>=, =current_tx: i64=

*** Implemented operations

- =:db/add= with cardinality enforcement (one: retract-old + add-new; many: skip duplicates)
- =:db/retract= with value (exact match, no-op if missing)
- =:db.fn/retractAttribute= / =:db/retract= without value (retract all =[e a]= datoms)
- =:db.fn/retractEntity= / =:db/retractEntity= (forward datoms + incoming refs + component cascade)
- Map entity expansion (mirrors CLJS =explode=) with reverse refs (=_parent=)
- Nested map entities (auto-allocated ref)
- Tempid resolution (negative numbers, strings) with consistent allocation
- Upsert via =:db.unique/identity= attributes
- Unique value constraint violation detection
- Lookup ref resolution in entity and value position
- =:db/current-tx= in entity and value position
- EDN tx-data parser using the =edn= crate

*** Not ported (CLJS fallback)

- =:db.fn/call= / custom transaction functions (require calling CLJS fns from Rust)
- =:db.fn/cas= / =:db/cas= (compare-and-swap — rarely used)
- Tuple attribute handling

*** Files

| File | Purpose |
|------+---------+
| =rust/persistent-sorted-set/src/transact.rs= | Core transact logic, =TransactableDB= trait, EDN parser |
| =rust/persistent-sorted-set/src/db.rs= | =impl TransactableDB for DataScriptDB= |
| =rust/datascript-rs/src/wasm_datascript.rs= | =impl TransactableDB for WasmDataScript= + WASM export |
| =rust/persistent-sorted-set/tests/transact_tests.rs= | 31 native tests |
| =src/absurder_sql/datascript/impl_rust.cljs= | =transact-rust!= CLJS bridge |

*** WASM API

=transact(txDataEdn: string): object= — mutates DB in place, returns:

#+begin_src javascript
db.transact('[[:db/add 1 :name "Alice"] {:db/id -1 :age 30}]')
// → { txData: [{e: 1, a: ":name", v: "Alice", tx: 536870913}, ...],
//     tempids: {"-1": 2},
//     currentTx: 536870913 }
#+end_src

*** Tests (31 native, =cargo test=)

Basic add, tempid resolution (neg/string), shared tempids, cardinality one
replacement, cardinality many, retract with value, retract nonexistent,
retract attribute, retract entity (+ incoming refs, + component cascade),
unique identity upsert, unique value violation, map entity expansion, nested
map entity, reverse ref, lookup ref, =:db/current-tx=, tx-report structure,
sequential transactions, EDN parsing integration (basic, map, retract,
retract entity, alias form, refs+components, mixed)

** 4.9 FilteredDB [LOW — NOT NEEDED for behave]

/Note:/ Behave audit found zero usage of =d/filter=. Deprioritized.

- *New file:* =rust/persistent-sorted-set/src/filtered_db.rs=

** 4.10 Incremental sync → superseded by 4.8

With 4.8 (Rust transact) implemented, =transact-rust!= in =impl_rust.cljs=
calls the Rust engine directly — no CLJS→Rust sync needed for supported tx
forms. =sync-to-rust!= remains as fallback for unsupported forms (=:db.fn/call=,
=:db.fn/cas=, custom tx functions).

** Phase 4 verification

1. Each sub-task has its own unit tests (=cargo test=)
2. WASM builds: =wasm-pack build --target web --no-typescript=
3. Full kaocha test suite passes with Rust engine handling all queries (no CLJS fallback)
4. Remove CLJS fallback path in =impl_rust.cljs= — all queries route through Rust
5. Performance benchmark: Rust-only path vs mixed CLJS/Rust path on production-scale data

* Phase 5: Behave Integration Parity

An audit of the behave app's actual DataScript usage reveals four gaps that must
be closed before the Rust engine can serve as a drop-in replacement. These are
ordered by effort (lowest first) to maximize incremental value.

Features confirmed *unused* by behave and therefore *not needed*: =:with=,
=:keys/:syms/:strs= return maps, tuple input bindings =[[?a ?b]]=, pull =:as=,
=:default=, =:limit=, =:xform=, recursive pull, =:db.fn/cas=, =:db.fn/call=,
=d/filter= (FilteredDB), =d/seek-datoms=, =d/index-range=, aggregates.

** DONE 5.1 =re-pattern= fn-expr [LOW]

*** Context

Behave's solver chains =re-pattern= and =re-find= as a two-step pattern:

#+begin_src clojure
;; solver/queries.cljs:158-160
[(str "(?i)" ?module-name) ?module-find]
[(re-pattern ?module-find) ?module-find-re]
[(re-find ?module-find-re ?m-name)]
#+end_src

The Rust engine's =re-find= already accepts string patterns, but =re-pattern= as
a fn-expr (which creates a regex from a string) is not implemented.

*** Implementation

- *File:* =rust/persistent-sorted-set/src/fn_expr.rs=
- Add ="re-pattern"= to =eval_fn=: return the string unchanged (identity)
  - In CLJS, =re-pattern= returns a RegExp object. In Rust, =re-find= already
    compiles the pattern from a string, so =re-pattern= is effectively identity.
- Add ="re-pattern"= to =is_known_fn=

*** Steps

1. Add ="re-pattern"= → =args.first().cloned()= case in =eval_fn= match block
2. Add ="re-pattern"= to the =is_known_fn= match list
3. Unit test in =fn_expr.rs=: =fn_re_pattern_identity= — =(re-pattern "abc")= → =Str("abc")=
4. Integration test in =parser_tests.rs=: =fn_expr_re_pattern_chain= —
   full query with =[(str "(?i)" ?name) ?pat] [(re-pattern ?pat) ?re] [(re-find ?re ?target)]=
   verifying case-insensitive matching works end-to-end
5. Update =QUERY_ENGINE_PLAN.org= fn-expr function list to include =re-pattern=

*Also fixed:* =apply_predicate= in =relation.rs= now collapses context when
predicate vars span multiple relations (needed for chained fn-expr → predicate
patterns like =[(re-pattern ?pat) ?re] [(re-find ?re ?name)]=).

- *Tests:* 1 unit test in =fn_expr.rs= (=fn_re_pattern_identity=) +
  2 integration tests in =parser_tests.rs= (=fn_expr_re_pattern_chain=,
  =fn_expr_re_pattern_no_match=)

** DONE 5.2 Collection input bindings =[?x ...]= [MEDIUM]

*** Context

Behave uses collection bindings extensively in =:in= clauses:

#+begin_src clojure
;; worksheet/subs.cljs:186
[:find [?g ...] :in $ % ?ws-uuid :where ...]

;; Also: collection *input* bindings (not just return):
;; worksheet/events.cljs — passes vectors of UUIDs as query inputs
#+end_src

Current Rust =bind_inputs= (=query_parser.rs:765=) only handles scalar bindings —
it substitutes =?var= → =Const(value)= in patterns. Collection bindings require a
different mechanism: each element creates a separate binding context, and results
are unioned.

*** Implementation

- *File:* =rust/persistent-sorted-set/src/query_parser.rs=
- *File:* =rust/datascript-rs/src/wasm_datascript.rs=

**** Parser changes

Extend =parse_in= to recognize binding forms:

#+begin_src
:in $ %                       → [SrcVar, RulesVar]
:in $ ?x                      → [SrcVar, ScalarBinding("?x")]
:in $ [?x ...]                → [SrcVar, CollBinding("?x")]
:in $ [[?a ?b]]               → [SrcVar, TupleBinding(["?a","?b"])]
#+end_src

Add =InBinding= enum to =ParsedQuery=:

#+begin_src rust
enum InBinding {
    Scalar(Var),
    Coll(Var),          // [?x ...] — one binding per element
    Tuple(Vec<Var>),    // [[?a ?b]] — destructure single tuple
}
#+end_src

**** Resolution changes

For collection bindings: instead of substituting the variable with a constant,
inject a new relation into the context containing one row per element:

#+begin_src rust
// For :in [?x ...] with input [1, 2, 3]:
// Create Relation { attrs: {"?x": 0}, tuples: [[1], [2], [3]] }
// Push into context before resolving where clauses
#+end_src

For tuple bindings: destructure the input tuple into individual variable bindings
and substitute as scalars.

**** WASM changes

Extend =queryEdn= input parsing to detect collection/tuple values (JS Arrays)
and pass them as the appropriate binding type.

*** Steps

1. Define =InBinding= enum (=Scalar=, =Coll=, =Tuple=) in =query_parser.rs=
2. Add =pub in_bindings: Vec<InBinding>= field to =ParsedQuery=
3. Update =parse_in= to detect binding forms:
   - =EdnValue::Symbol(?x)= → =InBinding::Scalar(?x)=
   - =EdnValue::Vector([Symbol(?x), Symbol("...")])= → =InBinding::Coll(?x)=
   - =EdnValue::Vector([Vector([?a, ?b])])= → =InBinding::Tuple([?a, ?b])=
4. Create =bind_collection_inputs= function:
   - For =Coll(?x)= with input =[1, 2, 3]=: build =Relation { {?x: 0}, [[1],[2],[3]] }=
   - For =Tuple([?a, ?b])= with input =[1, "x"]=: substitute as two scalars
   - For =Scalar=: delegate to existing =bind_inputs=
5. Update =resolve_query= to accept optional =initial_rels: Vec<Relation>= and
   pre-seed the context with collection-binding relations
6. Update =queryEdn= in =wasm_datascript.rs=: when an input value is a JS Array
   and the corresponding =InBinding= is =Coll=, build the relation instead of
   treating it as a scalar

*** Tests

- =parser_tests.rs=:
  - =parse_in_collection_binding=: =[:find ?e :in $ [?uuid ...] :where ...]= parses to =InBinding::Coll=
  - =parse_in_tuple_binding=: =[:find ?e :in $ [[?a ?b]] :where ...]= parses to =InBinding::Tuple=
  - =coll_binding_query=: 3-element vector input → 3 matching results
  - =coll_binding_empty=: empty vector input → 0 results
  - =tuple_binding_query=: =[1, "Alice"]= destructured → matching result

*** Documentation

- Update =QUERY_ENGINE_PLAN.org= parser section to note collection/tuple binding support
- Mark Phase 4.4 input bindings as complete (cross-reference from 5.2)

*Implementation:*
- =InBinding= enum (=Scalar=, =Coll=, =Tuple=) + =in_bindings= field on =ParsedQuery=
- =parse_in= returns =(Vec<String>, Vec<InBinding>)=, detects =[?x ...]=, =[[?a ?b]]=
- =build_collection_relations= creates initial relations for =Coll= bindings
- =resolve_query_with_initial= pre-seeds context with collection relations
- WASM =queryEdn= updated to split inputs into scalar/coll/tuple and route accordingly
- Empty collection → empty relation → zero results (correct DataScript behavior)
- *Tests:* 8 integration tests (parse coll/tuple/scalar/mixed bindings, query with
  coll join, empty coll, single element, coll+join)

** DONE 5.3 Database-aware fn-exprs: =get-else=, =get-some=, =missing?= [HIGH]

*** Context

Behave uses =get-else= in ~6 queries for optional attribute access with defaults:

#+begin_src clojure
;; worksheet/subs.cljs:506
[(get-else $ ?gv :group-variable/hide-result? false) ?hide-result]
#+end_src

Unlike pure fn-exprs (=str=, =+=, etc.), these functions take the database (=$=)
as their first argument and perform internal searches. In CLJS, they're
implemented in =built_ins.cljc:12-31=:

#+begin_src clojure
(defn- -get-else [db e a else-val]
  (if-some [datom (first (db/-search db [(db/entid db e) a]))]
    (:v datom) else-val))

(defn- -get-some [db e & as]
  (reduce (fn [_ a]
    (when-some [datom (first (db/-search db [(db/entid db e) a]))]
      (reduced [(:a datom) (:v datom)]))) nil as))

(defn- -missing? [db e a]
  (nil? (get (de/entity db e) a)))
#+end_src

*** Implementation

- *File:* =rust/persistent-sorted-set/src/fn_expr.rs= (or new =db_fn_expr.rs=)
- *File:* =rust/persistent-sorted-set/src/relation.rs=
- *File:* =rust/persistent-sorted-set/src/query_parser.rs=

**** Approach A: Extend =Clause::FnExpr= with resolver access

Add a new clause variant =Clause::DbFnExpr= that carries the function name, args,
binding variable, and a flag indicating it needs resolver access:

#+begin_src rust
Clause::DbFnExpr {
    name: String,       // "get-else", "get-some", "missing?"
    args: Vec<PatternEl>,
    binding: String,
}
#+end_src

In =resolve_clause_depth=, the =DbFnExpr= arm has access to the =resolver:
&R: PatternResolver= and can call =resolver.resolve_pattern()= internally.

**** =get-else= implementation

For each tuple, resolve =?e= and =?a= from context, search the database for
=[e a]=. If found, bind the value; if not, bind the default (last arg).

#+begin_src rust
fn apply_get_else<R: PatternResolver>(
    resolver: &R, ctx: &mut Vec<Relation>,
    args: &[PatternEl], binding: &str
) {
    // args[0] = $ (source var, ignored — resolver IS the db)
    // args[1] = ?e (entity ref variable)
    // args[2] = :attr (constant keyword)
    // args[3] = default-value (constant)
    for rel in ctx.iter_mut() {
        // For each tuple: search(e, a) → value or default
        // Append result as new column `binding`
    }
}
#+end_src

**** Parser changes

In =parse_pred_expr=, when the fn-expr name is =get-else=, =get-some=, or
=missing?=, produce =Clause::DbFnExpr= instead of =Clause::FnExpr=. The
distinguishing feature is that the first argument is a source variable (=$=).

*** Steps

1. Add =Clause::DbFnExpr { name, args, binding }= variant to =Clause= enum in =relation.rs=
2. Add match arms for =DbFnExpr= in:
   - =resolve_clause_depth= — dispatch to =apply_db_fn_expr(resolver, ctx, ...)=
   - =rename_clause= — alpha-rename args + binding
   - =bind_clause= — bind args from input parameters
3. Update =parse_pred_expr= in =query_parser.rs=:
   - When fn-expr has a binding AND first call arg is a source var (=$=):
     if name ∈ ={get-else, get-some, missing?}= → produce =Clause::DbFnExpr=
   - For =missing?= with no binding (predicate form): produce =DbFnExpr= with
     empty binding string (filter mode)
4. Implement =apply_db_fn_expr= (new function in =relation.rs= or =db_fn_expr.rs=):
   - *=get-else=:* for each tuple, resolve =?e= → entity ID, search
     =resolver.resolve_pattern([e, attr, _, _])=. If datom found → bind =(:v datom)=;
     if not → bind default (last arg). Append as new column.
   - *=get-some=:* for each tuple, try each attr arg in order; bind first found
     value as new column. Drop tuple if none found.
   - *=missing?=:* for each tuple, search =[e, attr]=; *keep* tuple only if NOT
     found (predicate filter, no new column).
5. Handle the =PatternResolver= dependency: =resolve_clause_depth= already has
   =resolver: &R= in scope, so the =DbFnExpr= arm can pass it directly.

*** Tests

- =parser_tests.rs=:
  - =parse_db_fn_get_else=: =[(get-else $ ?e :attr 0) ?val]= → =Clause::DbFnExpr=
  - =db_fn_get_else_found=: entity has attr → returns actual value (not default)
  - =db_fn_get_else_missing=: entity lacks attr → returns default value
  - =db_fn_get_else_false_default=: default is =false= → correctly returns =false= (not nil)
  - =db_fn_get_some_first=: first attr exists → returns its value
  - =db_fn_get_some_second=: only second attr exists → skips first, returns second
  - =db_fn_get_some_none=: no attrs exist → tuple dropped
  - =db_fn_missing_pred=: =[(missing? $ ?e :attr)]= correctly filters
  - =db_fn_combined=: query mixing =get-else= with patterns + predicates

*** Documentation

- Update =QUERY_ENGINE_PLAN.org= built-in predicates/functions to include =get-else=, =get-some=, =missing?=
- Add =db_fn_expr= to file inventory if a new module is created

*Implementation:*
- =Clause::DbFnExpr { name, args, binding }= variant in =relation.rs=
- Parser detects =get-else=, =get-some=, =missing?= by name and produces =DbFnExpr=
- =apply_db_fn_expr= dispatches to =apply_get_else=, =apply_get_some=, =apply_missing=
- Each function uses =resolver.resolve_pattern()= to search the DB per tuple
- =get-else=: appends column with found value or default
- =get-some=: tries attrs in order, drops tuple if none found
- =missing?=: predicate (no binding), filters tuples where attr IS found
- Context collapsed before evaluation so all vars are available
- *Tests:* 9 integration tests (parse, found/missing/false-default, get-some
  first/second/none, missing predicate, combined with other predicates)

** DONE 5.4 Multiple database sources (=$ $ws %=) [HIGH — ~8 hours]

*** Context

Behave's most complex queries cross two databases — VMS (vocabulary) and the
active worksheet:

#+begin_src clojure
;; solver/queries.cljs:23-24 — q-vms builds the query dynamically:
(apply d/q query-after @@vms-conn @@store/conn rules args)
;; where query-after = [:find ... :in $ $ws % ... :where [$ws ?e :attr ?v] ...]
#+end_src

The =$= prefix on patterns determines which database resolves them. =$ws= patterns
search the worksheet DB; unprefixed =$= patterns search the VMS DB.

*** Implementation

- *File:* =rust/persistent-sorted-set/src/query_parser.rs=
- *File:* =rust/persistent-sorted-set/src/relation.rs=
- *File:* =rust/datascript-rs/src/wasm_datascript.rs=

**** Parser changes

1. Parse source variables in =:in= clause: track =$=, =$ws=, etc. as named sources
2. Parse source variable prefixes on patterns: =[$ws ?e :attr ?v]= → attach source
   name ="$ws"= to the =Clause::Pattern=
3. Extend =Clause::Pattern= to carry an optional source name:

#+begin_src rust
Clause::Pattern {
    source: Option<String>,  // None = default ($), Some("$ws") = named source
    pattern: [PatternEl; 4],
}
#+end_src

**** Resolver changes

Replace the single =PatternResolver= with a =MultiResolver= that maps source
names to resolvers:

#+begin_src rust
struct MultiResolver<'a> {
    sources: HashMap<String, &'a dyn PatternResolver>,
}

impl PatternResolver for MultiResolver<'_> {
    fn resolve_pattern(&self, pattern: &[PatternEl; 4]) -> Relation {
        // Use default source "$"
        self.sources["$"].resolve_pattern(pattern)
    }

    fn resolve_pattern_from(&self, source: &str, pattern: &[PatternEl; 4]) -> Relation {
        self.sources[source].resolve_pattern(pattern)
    }
}
#+end_src

Update =resolve_clause_depth= to pass the source name from =Clause::Pattern= to
the resolver.

**** WASM changes

Extend =queryEdn= to accept multiple database inputs. The =$= (default) is =self=
(the WasmDataScript instance the method is called on). Additional sources (=$ws=)
are passed as arguments:

#+begin_src javascript
// New: queryEdnMulti(queryEdn, rulesEdn, inputs, additionalSources)
// where additionalSources = { "$ws": anotherWasmDataScript }
db.queryEdnMulti(
  '[:find ?x :in $ $ws % :where [$ws ?e :name ?x]]',
  rulesEdn, inputs,
  { "$ws": worksheetDb }
)
#+end_src

Alternatively, keep the existing =queryEdn= signature and add a separate method
for multi-source queries.

**** CLJS routing

In the integration layer (=core.cljc= or =impl_rust.cljs=), when =d/q= is called
with multiple DB inputs, detect which are Rust-backed and construct the multi-source
call:

#+begin_src clojure
;; If both DBs have registered Rust instances:
(.queryEdnMulti vms-rust-db query-edn rules-edn inputs
                #js {"$ws" ws-rust-db})
;; If only one has Rust, fall back to CLJS for the whole query
#+end_src

*** Steps

1. Extend =Clause::Pattern= from =([PatternEl; 4])= to
   ={ source: Option<String>, pattern: [PatternEl; 4] }=
2. Update *all* match sites for =Clause::Pattern= across:
   - =relation.rs=: =resolve_clause_depth=, =rename_clause=, =collapse_ctx=
   - =query_parser.rs=: =parse_pred_expr=, =bind_clause=, =parse_where_clause=
   - =wasm_datascript.rs=: =clause_from_js=
   - =tests/query_tests.rs=: all =Clause::Pattern(...)= constructors
   - =tests/parser_tests.rs=: all pattern matches
3. Update =parse_where_clause= to detect source-prefixed patterns:
   - When first vector element is =Symbol= starting with =$= and is not =?=:
     strip it as =source=, parse remaining 4 elements as pattern
4. Update =parse_in= to track source var names (=$=, =$ws=) in addition to
   binding vars
5. Create =MultiResolver= struct in =relation.rs=:
   - =sources: HashMap<String, &dyn PatternResolver>=
   - Implements =PatternResolver= (dispatches by source, default =$=)
   - Add =resolve_pattern_from(&self, source, pattern)= method
6. Update =resolve_clause_depth= to read =source= from =Clause::Pattern= and
   call =resolve_pattern_from= on the resolver
7. Add =queryEdnMulti= WASM export in =wasm_datascript.rs=:
   - Signature: =queryEdnMulti(query, rules, inputs, sources: JsValue)=
   - Parse =sources= JS object: each key (="$ws"=) maps to a =WasmDataScript= ref
   - Build =MultiResolver= with =self= as =$= and additional sources
8. Add CLJS routing in =impl_rust.cljs=:
   - In =q= function: detect multiple DB inputs from =:in= clause
   - Map each to its registered Rust instance
   - Call =.queryEdnMulti= if all have Rust; fall back to CLJS otherwise

*** Tests

- =parser_tests.rs=:
  - =parse_source_prefix_pattern=: =[$ws ?e :name ?n]= → =source: Some("$ws")=
  - =parse_default_source_pattern=: =[?e :name ?n]= → =source: None=
  - =multi_source_query=: two =DataScriptDB= instances, join across both
  - =multi_source_with_rules=: rule body references =$ws= source
  - =multi_source_with_predicate=: predicate after cross-source join
- =query_tests.rs= (optional):
  - =multi_resolver_dispatch=: verify correct DB is queried per source

*** Documentation

- Update =QUERY_ENGINE_PLAN.org= WASM API reference with =queryEdnMulti= signature + example
- Update =QUERY_ENGINE_PLAN.org= design decisions with multi-source architecture
- Note: this is the largest structural change — =Clause::Pattern= shape change
  touches many files. Consider doing this as a two-step commit:
  (a) refactor =Pattern= to struct form with =source: None= everywhere (no behavior change)
  (b) add actual multi-source resolution

** Phase 5 verification

For each sub-task (5.1–5.4):

1. =cargo test= — new + existing tests pass
2. =cargo check --target wasm32-unknown-unknown= — WASM compiles
3. =clj-kondo= — no CLJS errors

End-to-end parity:

4. Extract actual behave queries exercising each gap (from the audit)
5. Populate =DataScriptDB= with representative behave-like data
6. Run each query through Rust and CLJS engines, assert identical results

Documentation (after each sub-task):

7. =QUERY_ENGINE_PLAN.org= — file inventory, test counts, API reference updated
8. =DATASCRIPT_RUST.org= — mark sub-task DONE with implementation details

* Phase 6: Issues Before Removing CLJS Fallbacks

The Rust DataScript engine migration is functionally complete: all =d/q=, =d/pull=,
and =d/entity= calls in the behave app have been routed through =impl-rust/*=
with CLJS fallbacks. This phase tracks 8 remaining issues that must be resolved
before the CLJS fallback paths can be safely removed.

** TODO 6.1 Keyword Values Corrupted on Persistence Restore [HIGH]

*** Problem

=sync-from-rust= converts Rust datoms back to CLJS, but keyword values
(=:input=, =:output=, =:continuous=, =:discrete=, etc.) come back as strings
(=":input"=) instead of keywords (=:input=).

*** Root cause

=value_to_js= in =wasm.rs= converts =Value::Keyword= to a JS string =":input"=.
=sync-from-rust= in =impl_rust.cljs= passes =(.-v d)= as-is — no keyword parsing
for values (only attributes get =js-datom-attr->keyword=).

*** Why it doesn't break today

For query results, datoms from =sync-to-rust!= retain their =original_js=
reference (the original CLJS object), so =datom_to_js= returns the original value
with proper keywords. The issue ONLY manifests when datoms are reconstructed from
scratch — i.e., the persistence restore path.

*** Impact

If a worksheet is saved via Rust =.storeToLegacy= then restored via
=restoreFromLegacy= → =sync-from-rust=, keyword-valued attributes become strings.
Code like =(= (:submodule/io submodule) :input)= fails.

*** Fix

In =sync-from-rust=, parse value strings that look like keywords (start with =:=)
back into CLJS keywords, matching what =value_from_js= does on the Rust side.

*** Files

- =components/absurder_sql/src/absurder_sql/datascript/impl_rust.cljs= (~5 lines
  in =sync-from-rust=)

** TODO 6.2 No Error Handling in Rust Sync Listener [HIGH]

*** Problem

The =:rust-sync= listener in =setup-conn!= calls =sync-to-rust!= with no
try/catch. If it throws (WASM OOM, schema mismatch, etc.), the exception
propagates into DataScript's =transact!= internals, potentially leaving the CLJS
conn in an undefined state, and the Rust DB stale.

*** Fix

Wrap the listener body in =try=/=catch=. On failure, log a warning and optionally
set a flag (=rust-sync-failed?=) that =impl-rust/q= checks to force CLJS fallback
until the next successful sync.

*** Files

- =projects/behave/src/cljs/behave/store.cljs= (~8 lines in =setup-conn!=)

** TODO 6.3 reactive-entity Bypasses Rust Entirely [MEDIUM]

*** Problem

=austinbirch.reactive-entity= calls =(d/entity db eid)= directly (line 47 of
=reactive_entity.cljs=), plus =d/datoms= (line 53), =db/entid= (line 52),
=db/ref?= (line 58). It reads from the CLJS conn, not the Rust DB.

*** Why it matters

reactive-entity drives the worksheet entity subscriptions (=:worksheet= sub in
=worksheet/subs.cljs= line 55-62, used throughout the UI). It's performant because
it tracks fine-grained attribute access and only re-renders on change. It does NOT
benefit from Rust acceleration.

*** Impact for Phase 6

If we remove the CLJS DataScript DB entirely (making Rust the sole source of
truth), reactive-entity breaks completely.

*** Options

- *(A) Leave as-is:* reactive-entity uses CLJS DB (source of truth anyway). This
  is fine as long as we keep the dual-state model. No work needed now.
- *(B) Fork reactive-entity:* Replace =d/entity= with =impl-rust/entity-ws=.
  Requires RustDBProxy to fully support the entity protocols reactive-entity needs.
  More work, and reactive-entity is already fast for small worksheet DBs.

*** Recommendation

Option A — leave as-is. Document that reactive-entity requires the CLJS conn.

** TODO 6.4 Pull Not Accelerated Through Posh [MEDIUM]

*** Problem

Posh dcfg has =:q impl-rust/q= but =:pull* d/pull= (still CLJS). All
posh-managed =pull= subscriptions use CLJS DataScript.

*** Impact

=pull= subs (=vms/store.cljs=: =pull=, =pull-many=; used by =:vms/pull=,
=:vms/pull-many=, =:vms/pull-children= subs) don't benefit from Rust. These are
cached by posh (VMS never mutates), so performance impact is only on first render.

*** Fix

Replace =:pull* d/pull= with =:pull* impl-rust/pull= in =posh/reagent.cljs= dcfg.
Need to verify =impl-rust/pull= handles posh's expected return format.

*** Files

- =components/posh/src/posh/reagent.cljs= (~1 line)

** TODO 6.5 =d/entid= Calls Bypass Rust [LOW]

*** Problem

Two =d/entid= calls remain:

- =worksheet/subs.cljs:59= — =(d/entid @@s/conn [:worksheet/uuid ws-uuid])=
- =worksheet/events.cljs:624= — =(d/entid ds [:worksheet/uuid ws-uuid])=

These resolve lookup refs to numeric entity IDs via the CLJS DB.

*** Impact

Minor — these are not hot paths. But if we remove the CLJS DB, they break.

*** Fix

Add =impl-rust/entid= that uses =RustDBProxy='s =-datoms :avet= (now fixed with
correct index component mapping).

*** Files

- =impl_rust.cljs= (~10 lines)
- =worksheet/subs.cljs=, =worksheet/events.cljs= (~2 lines each)

** TODO 6.6 Listener Ordering Is an Implementation Detail [LOW]

*** Problem

=setup-conn!= relies on listeners firing in registration order (=:rust-sync=
before re-posh, re-posh before reactive-entity). This works because CLJS
=PersistentArrayMap= preserves insertion order for maps with ≤8 entries. But it's
not a documented guarantee.

*** Impact

If DataScript changes its listener storage or a 9th listener is added, ordering
breaks — posh queries run against stale Rust DB.

*** Fix

No code change needed now (≤3 listeners). Add a comment documenting the
dependency. If listener count grows, use a wrapper that enforces ordering.

*** Files

- =projects/behave/src/cljs/behave/store.cljs= (comment only)

** TODO 6.7 Dead Code Cleanup [LOW]

1. *=unsupported-find?=* — always returns =false=. Remove function and all call
   sites.
2. *=init-sql-conn-from-datoms!=* in =store.cljs= — never called. Remove.
3. *=-index-range=* on =RustDBProxy= — returns =nil=. Should throw to surface
   unexpected usage.

*** Files

- =impl_rust.cljs=, =store.cljs=

** TODO 6.8 Missing CLJS-Level Integration Tests [MEDIUM]

*** Problem

216 Rust-level tests pass, but there are NO ClojureScript-level tests that verify
the full interop chain: CLJS datom → =sync-to-rust!= → Rust query →
=rust-result->clj= → CLJS comparison. Keyword value round-trip, lookup ref entity
resolution via =RustDBProxy=, and posh reactive query re-evaluation are all
untested at the CLJS level.

*** Fix

Add integration tests to the kaocha test suite covering:

- Keyword value round-trip through sync-to-rust → query → result
- Lookup ref entity creation via =impl-rust/entity=
- =sync-from-rust= round-trip (save + restore preserves keyword values)
- Posh reactive sub re-fires correctly after transaction with =impl-rust/q= in
  dcfg

*** Files

- New test file in =test/absurder_sql/datascript/impl_rust_test.cljs=

** Phase 6 Priority Order

| # | Issue | Severity | Effort | Blocks Phase 6? |
|---+-------+----------+--------+-----------------|
| 1 | Keyword values on restore | HIGH | Small | Yes — persistence breaks |
| 2 | Sync listener error handling | HIGH | Small | Yes — silent data loss |
| 8 | CLJS integration tests | MEDIUM | Medium | Yes — can't verify correctness |
| 4 | Pull through posh | MEDIUM | Tiny | No — performance only |
| 3 | reactive-entity bypass | MEDIUM | None (document) | Partial — blocks full CLJS removal |
| 5 | d/entid bypass | LOW | Small | Yes — if removing CLJS DB |
| 6 | Listener ordering | LOW | Tiny (comment) | No |
| 7 | Dead code | LOW | Tiny | No |

** Phase 6 Verification

After addressing issues 6.1, 6.2, and 6.8:

- =cargo test= — all Rust tests pass
- kaocha CLJS tests — new integration tests pass
- Manual: create worksheet → enter data → save → reopen → verify keyword-valued
  attributes (e.g. =submodule/io=) display correctly
- Manual: toggle =window.toggleRustEngine()= off → identical behavior
- Manual: force sync failure (e.g. corrupt schema) → verify error logged, app
  continues functioning via CLJS fallback

** Key Files

- =components/absurder_sql/src/absurder_sql/datascript/impl_rust.cljs=
- =components/posh/src/posh/reagent.cljs=
- =projects/behave/src/cljs/behave/store.cljs=
- =projects/behave/src/cljs/austinbirch/reactive_entity.cljs= (reference only)
- =components/absurder_sql/rust/persistent-sorted-set/src/wasm.rs= (reference)
